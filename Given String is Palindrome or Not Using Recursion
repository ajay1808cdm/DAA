def is_palindrome(s):
    if len(s) <= 1:
        return True
    if s[0] != s[-1]:
        return False
    return is_palindrome(s[1:-1])
input_string = "madam"
print(f"{input_string} is a palindrome: {is_palindrome(input_string)}")  # Output: madam is a palindrome: True
